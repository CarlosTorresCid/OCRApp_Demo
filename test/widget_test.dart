import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Placeholder: ejecutar `flutter test` una vez configurado BFF_BASE_URL
    expect(true, isTrue);
  });
}
